# 数据血缘分析器 - 验收手册

> **验收结论标准**：所有 **必测项** 全部通过即为验收合格。带（可选）标记的项可按需抽查。

---

## 1. 构建与启动验收

| 步骤 | 操作 | 预期结果 | 类型 |
| :--- | :--- | :--- | :--- |
| Step 1 | 运行 `npm run build` | 命令退出码为 0；项目根目录下 `dist/` 和 `dist-electron/` 两个文件夹同时存在且非空 | **必测** |
| Step 2 | 运行 `npm start` | Electron 桌面窗口正常打开；顶部 Tab 菜单 8 个功能模块**全部可见**（数据源导入、脚本解析、血缘画布、节点搜索、影响评估、变更快照、报告导出、任务清单） | **必测** |
| Step 3（可选） | 运行 `npm run electron:build` | `release/` 目录生成 NSIS 安装包 `数据血缘分析器 Setup x.x.x.exe`；双击安装后桌面生成快捷方式；启动安装后的程序，界面与 Step 2 一致 | 可选 |

**补充验证**：
- `dist/` 内必须包含 `index.html` 及 assets 资源
- `dist-electron/` 内必须包含 `main.js` 和 `preload.js`
- 启动窗口后无控制台报错（按 F12 打开开发者工具）

---

## 2. 持久化回归验收（必测 6 步）

> **验收原则**：每一步操作后执行 **手动刷新**（Ctrl+R / F5）或**关闭再重开窗口**，数据必须仍然存在。持久化基于 localStorage。

### Step 1：清空数据 → 空状态持久化

| 操作 | 预期结果 |
| :--- | :--- |
| 切换到「数据源导入」面板 → 点击「清空全部数据」按钮 → 确认弹窗 | 画布节点全部消失，节点列表为空，Tab 计数归零 |
| 按 F5 刷新页面 | 刷新后画布仍为空，节点列表仍为空（说明"空状态"也被正确持久化） |

### Step 2：数据源导入 → 节点持久化

| 操作 | 预期结果 |
| :--- | :--- |
| 在「数据源导入」面板 → 手动新建节点 → 选择类型 `table` → 填写表名（如 `ods_order`）→ 添加至少 3 个字段（带 type、isKey 等属性）→ 提交保存 | 节点列表新增 1 条记录；数据源 Tab 角标计数 **+1**；画布上出现该表节点 |
| 按 F5 刷新页面 | 刷新后表节点仍在，字段属性完整，Tab 计数不变 |

### Step 3：任务清单 → 任务持久化

| 操作 | 预期结果 |
| :--- | :--- |
| 切换到「任务清单」面板 → 新建任务 → 填写标题（如 `ods_order 字段变更`）→ 优先级选 `high`（高）→ 关联 Step 2 中的表节点 → 提交 | 任务列表新增 1 条记录，高优先级有红色/橙色高亮标识 |
| 按 F5 刷新页面 | 刷新后任务仍存在，优先级、关联节点信息不丢失 |

### Step 4：变更快照 → 快照持久化

| 操作 | 预期结果 |
| :--- | :--- |
| 切换到「变更快照」面板 → 创建快照 → 输入名称（如 `验收基线快照`）→ 填写描述（如 `持久化验收 Step 4 基线`）→ 确认保存 | 快照列表新增 1 条，显示名称、描述、创建时间，包含节点数和边数 |
| 按 F5 刷新页面 | 刷新后快照仍在，节点/边的数量与创建时一致 |

### Step 5：示例数据按钮 → 合并不覆盖

> 本步核心验证：**导入示例数据时不覆盖已存在的真实数据**。

| 操作 | 预期结果 |
| :--- | :--- |
| 在「数据源导入」面板点击「加载示例数据」按钮 | 示例数据追加导入，不覆盖 Step 2 手动创建的表 |
| 查看数据源 Tab 计数 | **表总数 > 示例数据的表数**（若示例数据有 N 张表，则总数应为 N+1 以上） |
| 定位 Step 2 创建的 `ods_order` 表 | 该表及其字段仍然存在，属性未被修改 |

### Step 6：关闭重开 → 跨进程持久化

| 操作 | 预期结果 |
| :--- | :--- |
| 关闭 Electron 桌面窗口（右上角 ×） | 窗口关闭 |
| 重新运行 `npm start` 启动 | 窗口重新打开 |
| 依次检查三类数据 | ① Step 2 的表节点及字段存在 ② Step 3 的高优先级任务存在 ③ Step 4 的快照存在 |

> ✅ **以上 Step 1 ~ Step 6 全部通过，则持久化回归验收合格。**

---

## 3. 功能验收要点

以下为各功能模块的**核心验收点**，建议按业务场景逐项走查。

### 3.1 脚本解析精度

- 支持 **CREATE TABLE / INSERT INTO ... SELECT / UPDATE / MERGE** 等常见 DDL/DML 语法
- 多语句脚本（以分号分隔）能正确解析出多条血缘关系
- 解析结果中 `table` 节点自动生成，字段从 `CREATE TABLE` 中提取
- 解析失败时有明确错误提示（红色 Toast + 行号定位），不会导致应用崩溃

### 3.2 字段级血缘高亮

- 画布上选中一条边（连接关系），能在右侧/悬浮层查看 **sourceField → targetField** 映射
- 选中某个表节点展开字段列表，点击某字段后：
  - 所有依赖该字段的下游字段**高亮**（如橙色边框）
  - 所有流入该字段的上游字段**高亮**（如蓝色边框）
- 高亮状态在画布缩放、拖拽后不丢失

### 3.3 影响评估字段级追踪

- 在「影响评估」面板选择一个表节点（或具体字段）→ 点击「分析影响」
- 输出结果包含：
  - 受影响节点列表（含类型、负责人、是否核心节点）
  - 受影响字段明细（哪个节点的哪个字段受影响）
  - 风险等级（`high` / `medium` / `low`）
- 支持「一键生成任务」→ 受影响的每一项自动生成一条 `TaskItem` 并写入任务清单

### 3.4 快照对比（节点 + 字段级）

- 在「变更快照」面板选择**两个不同时间点的快照** → 点击「对比」
- 对比结果按维度分组展示：
  - **新增节点** / **删除节点** / **修改节点**（节点级）
  - **新增字段** / **删除字段** / **修改字段**（字段级，含修改前后属性 diff）
  - **新增边** / **删除边**（关系级）
- 每一条变更记录可点击「定位」→ 画布自动滚动到对应节点并高亮

### 3.5 报告导出（两种模板 + Excel 分 sheet）

- **报告导出**面板提供至少两种 Word 模板：
  - 模板 A：《数据血缘分析报告（完整版）》—— 含全量节点、关系、快照摘要
  - 模板 B：《变更影响评估报告》—— 仅含指定影响分析的结果 + 任务清单
- Excel 导出为 **多 Sheet 工作簿**，至少包含：
  - Sheet1：数据节点清单
  - Sheet2：字段明细
  - Sheet3：血缘关系边
  - Sheet4：任务清单
  - Sheet5：快照列表
- 导出文件命名含时间戳，下载后可正常打开无损坏

### 3.6 数据源导入合并策略

- 导入 JSON/Excel 数据源时提供三种合并策略可选择：
  - **merge（合并）**：按节点 id/name 匹配，存在则更新字段，不存在则新增
  - **overwrite（覆盖）**：先清空现有数据再导入
  - **skip（跳过）**：已存在的节点跳过不处理，仅新增
- 导入完成后显示统计：新增 X 条 / 更新 Y 条 / 跳过 Z 条 / 失败 N 条

---

## 4. 问题排查

### localStorage 被清空怎么办？

应用所有业务数据（节点、边、快照、任务）均存储在浏览器 localStorage 中，Key 为 `data-lineage-store`。

| 场景 | 排查方法 | 处理方式 |
| :--- | :--- | :--- |
| 手动误点「清空全部数据」 | 无撤销功能 | 从最近的快照恢复（变更快照 → 选中快照 → 恢复数据） |
| 浏览器/系统清理缓存导致 localStorage 被清 | 按 F12 → Application → Local Storage 检查 Key 是否存在 | 若有备份的导出报告/JSON，通过「数据源导入」面板重新导入 |
| 重装安装包后数据丢失 | Electron localStorage 位于用户目录 `%APPDATA%/数据血缘分析器` | 重装前备份该目录，或养成定期导出快照/报告的习惯 |

> 💡 **最佳实践**：重要里程碑节点务必创建「变更快照」并导出 JSON 备份。

---

### 构建中断怎么查 TS 错误

执行 `npm run build` 或 `npm run build:web` 时若 TypeScript 编译失败，终端会输出错误信息。

**推荐排查流程**：

1. 终端定位到**第一条** `error TSxxxx:` 错误（TS 错误会级联，修第一条通常能解决后续多条）
2. 根据错误信息中的**文件路径 + 行号**打开对应文件修复
3. 修复后单独执行 `npm run build:web` 快速验证前端构建（比全量 build 快）
4. 若主进程报错，单独执行 `npm run build:electron` 验证 `electron/*.ts`

常用快速定位命令：
```bash
# 仅检查前端 TS 类型，不产出文件（最快）
npx tsc --noEmit

# 检查主进程 TS 类型
npx tsc -p tsconfig.electron.json --noEmit
```

---

### 打包安装包启动白屏排查

`npm run electron:build` 打出的安装包，用户安装后启动白屏，按以下顺序排查：

| 排查顺序 | 检查项 | 可能原因 & 处理 |
| :--- | :--- | :--- |
| ① | 安装目录下 `resources/app/` 是否存在 `dist/` 和 `dist-electron/` | 缺失说明 `electron-builder` 的 `files` 配置漏打包；检查 `package.json` build.files 是否包含 `dist/**/*` 和 `dist-electron/**/*` |
| ② | 启动白屏后按 `F12` / `Ctrl+Shift+I` 调开发者工具 | 打开 Console 查看具体报错；常见的有资源路径 404（`vite.config.ts` 的 `base` 需为 `./`） |
| ③ | 检查 asar 打包是否损坏 | 在 `package.json` build 中临时设 `"asar": false` 重打一次，对比是否正常；若正常则是 asar 打包问题，检查是否有原生依赖 |
| ④ | 对比 `npm start` 本地是否白屏 | 本地启动正常但安装包白屏 → 大概率是打包路径/asar 问题；本地也白屏 → 参考 DEVELOPMENT.md 「启动白屏」章节排查 |
